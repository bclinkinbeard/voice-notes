{
  "name": "voice-notes",
  "private": true,
  "version": "20.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "node server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node tests.js && node server/sync-tests.js && node server/sync-blob-tests.js",
    "test:server": "node server/sync-tests.js && node server/sync-blob-tests.js"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.4.0",
    "@vercel/blob": "^2.3.1"
  }
}
